[
  {
    "id": "demo-sandbox",
    "claim": "The demo starts with two sample scores and can be reset without saving data.",
    "where": "landing primary action and demo banner",
    "test": "npm test -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser contexts for landing-click /demo, /?demo=1&license=demo-license-sentinel, and /demo?license=demo-license-sentinel; reset, inspect storage, preserve a pre-existing real-license sentinel, and record outgoing requests"
  },
  {
    "id": "geometric-feedback",
    "claim": "Each stroke returns its average distance from the target.",
    "where": "landing live system and practice gauge",
    "test": "npm test -- --grep @claim:geometric-feedback",
    "sandbox": "fresh browser context at /demo; draw the line target and assert a numeric reading"
  },
  {
    "id": "five-core-free",
    "claim": "Five core drills are free.",
    "where": "landing facts and paid tier",
    "test": "npm test -- --grep @claim:five-core-free",
    "sandbox": "fresh browser context at /practice; assert five enabled core drills and three locked pack drills"
  },
  {
    "id": "local-practice",
    "claim": "Practice strokes and scores stay in the current tab.",
    "where": "landing facts, boundaries, privacy page, README",
    "test": "npm test -- --grep @claim:local-practice",
    "sandbox": "fresh browser context at /demo; intercept the full drawing flow and inspect localStorage"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts and README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /?demo=1 against a production preview; wait for service-worker control and cached shell assets, set the context offline, reload, then draw and assert a score"
  },
  {
    "id": "input-methods",
    "claim": "The drawing area accepts pen, mouse, touch, and keyboard arrows.",
    "where": "landing steps and drawing area instructions",
    "test": "npm test -- --grep @claim:input-methods",
    "sandbox": "fresh browser context at /practice; score keyboard input and synthetic mouse, pen, and touch Pointer Events"
  },
  {
    "id": "license-restore",
    "claim": "Existing Space Pack licenses can be restored by pasting the token.",
    "where": "practice desk restore control",
    "test": "npm test -- --grep @claim:license-restore",
    "sandbox": "fresh browser context at /practice; mock a valid Sociobot verification response, paste a token, and assert the three pack drills unlock"
  },
  {
    "id": "license-storage",
    "claim": "A supplied license stores only its token and last check result; after a valid check, it waits a day before checking again.",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:license-storage",
    "sandbox": "fresh browser context at /practice; assert empty storage before input, exact keys after restore, no repeat check within a day, and a new check after one day"
  },
  {
    "id": "license-verification-privacy",
    "claim": "License verification sends the token you paste to Sociobot.",
    "where": "privacy page",
    "test": "npm test -- --grep @claim:license-verification-privacy",
    "sandbox": "fresh browser context at /practice; record the full drawing and restore flow, then assert the only cross-origin request is a bodyless Sociobot verify GET containing exactly the pasted token"
  },
  {
    "id": "five-minute-session",
    "claim": "Each practice desk starts with a five-minute timer.",
    "where": "landing headline and practice header",
    "test": "npm test -- --grep @claim:five-minute-session",
    "sandbox": "fresh browser context at /practice; assert the displayed timer starts at 05:00"
  },
  {
    "id": "account-free",
    "claim": "The five free drills do not require an account.",
    "where": "terms page",
    "test": "npm test -- --grep @claim:account-free",
    "sandbox": "fresh browser context at /practice; assert the five core drills load without sign-in UI or stored account data"
  }
]
